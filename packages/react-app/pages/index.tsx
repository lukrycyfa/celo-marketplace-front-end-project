// This is the main page of the app

// Import the ProductList components
import ProductList from "@/components/ProductList";

// Export the Home component
export default function Home() {
  return (
    <div>
        <ProductList />
    </div>
  )
}